# job-application-ts
A full-stack job application tracker built with React + TypeScript (frontend) and Node.js + Express (backend), using Supabase PostgreSQL for data storage and authentication. Track job applications, interview rounds, and application status in one place.
